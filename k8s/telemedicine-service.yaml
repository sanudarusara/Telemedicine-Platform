apiVersion: v1
kind: Service
metadata:
  name: telemedicine-service
spec:
  type: NodePort
  selector:
    app: telemedicine-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30050