apiVersion: v1
kind: Service
metadata:
  name: appointment-service
spec:
  type: NodePort
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30001
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
spec:
  type: NodePort
  selector:
    app: appointment-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30061
