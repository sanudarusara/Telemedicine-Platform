# Appointment Service

Placeholder appointment service. Mirrors structure of other microservices in this repo.

Endpoints:
- `POST /appointments/create` - create an appointment
- `GET /appointments` - list appointments
- `GET /appointments/:id` - get appointment by id

Run:

```bash
npm install
npm run dev
```
